# 提示词系统说明

本文档用于说明本项目中提示词（Prompt）模板的组织方式、功能分工与维护原则，便于在 GitHub 中快速理解整套自动化文献综述生成系统的提示词设计。

## 1. 总体定位

本项目的提示词模板服务于一个面向自动化文献综述生成的知识增强多智能体协同框架。整套系统并非依赖单一提示词一次性完成综述写作，而是将任务拆分为多个阶段，由不同模块分别完成文献获取、知识组织、内容生成与质量治理。

为便于 GitHub 展示与附录整理，当前仓库额外提供了一个统一整理后的目录：

- `paper_project/docs/prompt_package/`

该目录中已经按功能分组复制了主工作流与对比实验中使用的主要提示词文件，并补充了说明文档。原始提示词仍保留在代码实现目录中，但 GitHub 阅读建议优先以 `prompt_package` 为入口。

## 2. 提示词的功能分层

为便于理解，本文将项目中的提示词模板划分为五组：

1. 对比实验提示词
2. 知识获取类智能体提示词
3. 知识组织类智能体提示词
4. 内容生成类智能体提示词
5. 知识治理类智能体提示词

这种划分与论文正文中的系统结构保持一致，只是在 GitHub 展示时额外将两个基线实验模板单独列为一组，便于与 full 工作流区分。

## 3. 五组提示词的职责

### 3.1 对比实验提示词

这组提示词不属于 full 工作流的主链路，而是用于实验对照。

- `Direct-Gen / AutoGen`：不给出分阶段规划链路，直接生成整篇综述正文。
- `RAG-Gen / RAGBase`：基于检索材料直接生成整篇综述正文。

这类提示词主要用于和多阶段协同工作流进行比较，观察结构规划、规范约束与生成后治理是否带来收益。

### 3.2 知识获取类智能体提示词

这组提示词负责从外部文献中获取信息，并将其转化为结构化中间知识。它既包括“面向主题论文的知识获取”，也包括“面向高质量综述的写作规范获取”。

典型任务包括：

- 关键词扩展与补检索
- 论文类型识别
- 属性树抽取
- 章节边界识别
- 章节画像抽取
- 章节类型归一化
- 结构规范、挂载规范、论断模板与引用策略抽取
- 将当前大纲映射到规范库

这组提示词的输出通常会进入共享知识平台，供后续模块继续读取和复用。

### 3.3 知识组织类智能体提示词

这组提示词负责将已有材料整理成可写作的结构化框架。

典型任务包括：

- 生成候选一级大纲
- 合并多个候选大纲
- 将论文挂载到一级大纲
- 生成二级大纲
- 将论文挂载到二级大纲

它们的目标不是直接写正文，而是形成“章节结构 - 证据材料”之间的对应关系。

### 3.4 内容生成类智能体提示词

这组提示词负责根据大纲、挂载记录与规范约束生成综述正文。

典型任务包括：

- 按小节逐步生成正文
- 生成章节引导性文本
- 生成摘要
- 对正文进行局部一致性与语言层面的精炼

### 3.5 知识治理类智能体提示词

这组提示词主要作用于正文生成之后，负责对关键表述进行审查与修订。

典型任务包括：

- 论断分解
- 证据核查
- 补充引用
- 对证据不足的表述进行改写

它们的核心目标是减少 unsupported claim、提高引用匹配程度，并增强生成结果的可追溯性。

## 4. 提示词与工作流阶段的对应关系

大致对应关系如下：

- 检索阶段：关键词扩展、补检索、主题描述、论文类型分类
- 知识抽取阶段：属性树抽取、章节画像与规范抽取
- 大纲阶段：一级大纲生成、候选合并、二级大纲生成
- 挂载阶段：一级挂载、二级挂载
- 正文生成阶段：分小节写作、章节文字生成、摘要生成、LCE 精炼
- 治理阶段：论断分解、证据核查、补引、改写

因此，提示词并不是孤立使用的，而是嵌入到完整工作流中，以阶段化方式逐步推动综述生成。

## 5. 使用与维护建议

### 5.1 使用原则

- 以代码中实际调用的提示词文件为准。
- 修改提示词前，应先确认其所处阶段与上游输入、下游输出。
- 对比实验提示词与主工作流提示词应分开维护，避免实验设置混入正式流程。

### 5.2 维护原则

- 优先保持提示词职责单一，不将多个阶段目标混在同一模板中。
- 提示词修改后，应同步检查对应的 IO 记录与实验结果。
- 若论文中的模块命名发生变化，应优先更新整理文档与说明文档，而不是直接改动提示词原文。

### 5.3 附录与 GitHub 展示的区别

- 论文附录更强调“可引用”和“可展示”，通常需要保留提示词原文。
- GitHub 文档更强调“可理解”和“可维护”，更适合先给出分类说明，再提供路径与对应关系。

因此，本仓库建议同时保留：

- 一份用于论文附录的完整提示词整理文件
- 一份用于 GitHub 展示的提示词说明与目录文件

## 6. 配套文档

与本文档配套的提示词目录清单见：

- `paper_project/docs/prompt_package/prompt_catalog.md`

若需要查看论文附录式的完整整理版本，可参考已有的附录类文档。
